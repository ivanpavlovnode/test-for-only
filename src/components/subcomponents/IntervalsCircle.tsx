import React , {useContext} from 'react';
import { useAppContext } from '../AppBlock';

const IntervalsCircle = () => {
  const {chosenInterval, intervals, setChosenInterval} = useAppContext();
  return (
    <div>
    </div>
  );
};
export default IntervalsCircle;